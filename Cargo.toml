[package]
name = "little-book-of-deep-learning-notes-cn"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { git = "https://github.com/tracel-ai/burn.git", branch = "main", features = ["ndarray", "flex"] }